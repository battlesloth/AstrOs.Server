# Code Review Phase 1: Quick Wins

Light plan — trivial 1-3 line fixes with no architectural impact.

## Tasks

- [x] Fix typos in `useHumanCyborgRelations.ts` (L90, L104)
- [x] Remove leading slash from `SYNC_CONFIG` in `api/endpoints.ts`
- [x] Add blob URL revocation in `UtilityView.vue` log download
- [x] Rename `handelWebsocketMessage` → `handleWebsocketMessage` in `api_server.ts`
- [x] Change login error status 404 → 500 in `authentication_controller.ts`
