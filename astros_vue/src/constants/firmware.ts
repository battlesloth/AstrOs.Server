// Mirror of astros_api/src/models/firmware_config.ts so tooltips on the
// modules page can show the required minimum without an extra round-trip.
// The server is the source of truth for compatibility; this constant is
// only used for display. Keep in sync with the API value when bumping.
export const MINIMUM_FIRMWARE_VERSION = '1.2.0';
