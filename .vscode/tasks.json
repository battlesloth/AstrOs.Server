{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "copy .env",
            "type": "shell",
            "command": "echo Hello",
            "windows":{
                "command": "cmd",
                "args": ["/C", "if not exist ${workspaceFolder}/api/dist/ mkdir ${workspaceFolder}/api/dist/ && copy ${workspaceFolder}/.env ${workspaceFolder}/api/dist/.env "]         
            }
        }
    ]
}